```
本章要点：
（1）单例模式确保程序中一个类最多只有一个实例。
（2）单例模式也提供访问这个实例的全局点。
（3）在 Java 中实现单例模式需要私有的构造器、一个静态方法和一个静态变量。
（4）确定在性能和资源上的限制，然后小心的选择适当的方案来实现单例，以解决多线程问题（即 必须
认为所有的程序都是多线程的）。
（5）如果不是采用 Java 5 以及以上的版本，双重检查加锁实现会失效。
（6）注意，如果使用多个类加载器，可能导致单例失效而产生多个实例。
（7）如果使用 Java 1.2 或之前的版本，必须建立单例注册表，以避免垃圾收集器将单例回收。
```
