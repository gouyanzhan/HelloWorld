## 第一章 栈和队列

```
1、设计一个有 getMin 功能的栈（士）
2、由两个栈组成的队列（尉）
3、如何仅使用递归函数和栈操作逆序一个栈（尉）
4、猫狗队列（士）
5、用一个栈实现另一个栈的排序（士）
6、用递归解决汉诺塔问题（校）
7、生成窗口最大值数组（尉）
8、构造数组的 MaxTree（校）
9、求最大子矩阵的大小（校）
10、最大值减去最小值小于等于 num 的子数组数量（校）
```